# feedback

In-tree Python port of the historical `feedback.pl` script: generates the pipeline
feedback HTML page(s). Invoked by `pipeline.pipeline_feedback` at the `postpipe` step.

::: evn_postprocess.feedback
    options:
      show_root_heading: true
      members_order: source
      show_source: false
