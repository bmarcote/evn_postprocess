# vex

Minimal VEX-format parser used by `inputs` and `experiment` to read `.vex`/`.vix` files.

::: evn_postprocess.vex
    options:
      show_root_heading: true
      members_order: source
      show_source: false
