# reporting

Per-step output on three independent channels: the Rich terminal, the loguru debug
file (`logs/logging_messages.log`), and the replayable command log (`logs/commands.sh`).

::: evn_postprocess.reporting
    options:
      show_root_heading: true
      members_order: source
      show_source: false
